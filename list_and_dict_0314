#2019实战第二期-列表字典书打卡
##列表list    

列表可以修改，元组不能修改

通用序列的操作: 索引、切片、相加、相乘和成员资格

切片：第一个索引是包含的第一个元素的编号,但第二个索引是切片后余下的第一个元素的编号. 切片可以用步长.

###列表的操作:
1. 修改列表：给元素赋值
2. 删除元素用del
3. 给切片赋值

列表的方法:
append, clear, copy, count, extend. index, insert, pop, remove, reverse, sort(sort 修改且不返回任何值), 
高级排序(key, reverse)

###字典的使用:

items -> key: value
 
函数dict

键可以是任何不可变的类型

字典的方法:
clear(删除,什么都不返回), copy浅拷贝, deepcopy深复制, fromkeys创建一个新字典　dict.fromkeys(),
get -> 当键不存在的时候不会引发异常，而是返回None, 可以指定默认值. d.get('name', 'N/A'),
items()返回一个包含所有字典项的列表, keys, pop->用于获取与指定键相关联的值并将该键值对从字典中删除, 
popitem->随机地弹出一个字典项(无序)，　setdefault类似get, 
update对于通过参数提供的字典,将其项添加到当前字典中。如果当前字典包含键相同的项,就替
换它.